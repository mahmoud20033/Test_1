# MongoDB Connection String
# Get this from MongoDB Atlas: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://admin:password@cluster0.mongodb.net/mydb?retryWrites=true&w=majority

# Node Environment
# Options: development, production
NODE_ENV=production

# Server Port (Heroku and cloud platforms override this)
PORT=8080

# CORS Configuration
# Update with your production domain
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,http://localhost:5173,http://localhost:3000

# Optional: JWT Secret for token signing
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Optional: API Keys or other sensitive data
# API_KEY=your-api-key-here
